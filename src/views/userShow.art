{{each data}}
<tr>
    <td>{{$index+1}}</td>
    <td>{{$value.username}}</td>
    <td>
        <button type="button" class="btn btn-block btn-danger btn02" 
        data-id="{{$value._id}}">删除</button>
    </td>
</tr>
{{/each}}